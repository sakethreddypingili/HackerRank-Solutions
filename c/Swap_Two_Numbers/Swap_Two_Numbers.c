#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    
    // Print swapped values directly (no actual variable swapping needed)
    printf("a = %d\nb = %d", b, a);
    
    return 0;
}
